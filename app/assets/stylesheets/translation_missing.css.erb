<% if Rails.env.development? %>
  .translation_missing {
    border: 3px dashed red;
  }
<% end %>

<% if Rails.env.production? %>
  .translation_missing {
    border: 3px dashed red;
  }
<% end %>
